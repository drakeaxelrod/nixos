# Host-specific secrets for honeypot (pentest machine)
#
# SETUP INSTRUCTIONS:
# 1. Generate age key from host SSH key:
#    nix-shell -p ssh-to-age --run 'cat /etc/ssh/ssh_host_ed25519_key.pub | ssh-to-age'
# 2. Store this age key in Bitwarden/Proton Pass as "honeypot-age-key"
# 3. Copy this file to secrets.yaml and fill in real values
# 4. Encrypt: sops -e -i secrets.yaml
# 5. Commit encrypted secrets.yaml to git

# Pentest credentials
metasploit_db_password: your-msf-db-password
burp_suite_license: your-burp-license-key
vpn_credentials: your-vpn-credentials

# WiFi passwords
wifi_password: your-wifi-password

# User passwords (mkpasswd -m sha-512)
root_password_hash: $6$rounds=656000$...
bamse_password_hash: $6$rounds=656000$...

# Disk encryption
luks_password: your-disk-encryption-password

# API keys for security tools
shodan_api_key: your-shodan-api-key
virustotal_api_key: your-virustotal-api-key
